version: '3.8'

services:
  proxy:
    build: ./
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: 1
    ports:
      - 80:80
      - 443:443
    volumes:
      - "./proxy:/proxy"
    container_name: proxy

  db:
    image: mongo:latest
    ports:
      - 27017:27017
    volumes:
      - ./db:/data/db
    container_name: ctf_db

networks:
  default:
    name: ctf_network